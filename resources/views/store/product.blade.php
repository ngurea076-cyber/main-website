@extends('layouts.store') @section('title',$product->title) @section('content')<section class="mx-auto grid max-w-6xl gap-10 px-4 py-12 md:grid-cols-2"><div class="rounded-3xl bg-white p-8"><img src="{{ $product->primary_image }}" class="h-full max-h-[500px] w-full object-contain" alt="{{ $product->title }}"></div><div><p class="font-semibold uppercase text-brand">{{ $product->brand }}</p><h1 class="mt-2 text-4xl font-black">{{ $product->title }}</h1><p class="mt-5 text-3xl font-black">KES {{ number_format($product->price) }}</p><p class="mt-6 leading-7 text-gray-600">{{ $product->description }}</p><a href="https://wa.me/{{ config('services.whatsapp.number','254700000000') }}?text={{ urlencode('Hello, I am interested in '.$product->title) }}" class="mt-8 inline-flex rounded-full bg-brand px-7 py-3 font-bold text-white">Enquire on WhatsApp</a></div></section>@endsection
